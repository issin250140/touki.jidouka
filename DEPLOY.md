# 特定の人にだけ共有する（公開デプロイ手順）

このツールを「特定の人にだけパスワード付きで共有し、自分でいつでも
（1人単位でも）止められる」状態にするための手順です。無料のホスティング
サービス **Render** を使います（他のDocker対応サービス、例えば Railway や
Fly.io でも同じ `Dockerfile` がそのまま使えます）。

アカウント作成やパスワードの入力はご自身で行ってください
（セキュリティ上、この作業は代行できません）。ここではその手順を説明します。

---

## 0. 事前に決めておくこと

共有したい人ごとに、**ユーザー名とパスワードの組**を決めます。

例:
```
tanaka:8x2kQmZp
sato:p9Lm3vBq
yamada:qR7wZ2xT
```

パスワードは推測されにくい、意味のないランダムな文字列がおすすめです。
共有したい相手には、**URL・その人専用のユーザー名・パスワード**の3つを伝えます
（全員に同じものを配るのではなく、1人ずつ別のものを渡すのがポイントです。
そうすることで、後から1人だけアクセスを止められます）。

## 1. GitHubにコードを置く

1. [github.com](https://github.com) でアカウントを作成（お持ちでなければ）。
2. 右上の「+」→「New repository」で新しいリポジトリを作成
   （例: `land-finder`、Public/Privateどちらでも可）。
3. このフォルダの中身をそのリポジトリにアップロードします。ターミナルで
   このフォルダに移動し、以下を実行してください（`<GitHubのURL>` は
   GitHubがリポジトリ作成後に表示するものに置き換えてください）。

   ```bash
   git init
   git add .
   git commit -m "土地探しツール"
   git branch -M main
   git remote add origin <GitHubのURL>
   git push -u origin main
   ```

## 2. Renderでアカウントを作り、デプロイする

1. [render.com](https://render.com) でアカウントを作成し、GitHubアカウントと連携します。
2. ダッシュボードで「New +」→「Blueprint」を選び、先ほどのGitHubリポジトリを選択します。
   （このリポジトリには `render.yaml` が入っているため、設定が自動で読み込まれます）
3. 環境変数の入力を求められたら、`SITE_USERS` に決めた組み合わせを
   カンマ区切りで入力します。

   ```
   tanaka:8x2kQmZp,sato:p9Lm3vBq,yamada:qR7wZ2xT
   ```

4. 「Apply」でデプロイが始まります（初回はChromiumのインストールも含むため、
   数分かかります）。
5. 完了すると `https://land-finder-xxxx.onrender.com` のようなURLが発行されます。
   これが共有用のURLです。

   ※ Render無料プランは「一定時間アクセスが無いとスリープし、次回アクセス時に
   再起動して数十秒かかる」仕様です。頻繁に使う場合は有料プランへの変更をご検討ください。

## 3. 特定の人にだけ共有する

発行されたURL＋その人専用のユーザー名・パスワードを、1人ずつ個別に伝えてください
（例: メッセージアプリで個別に送る）。組み合わせを知らない人はアクセスできません。

## 4. 特定の1人だけアクセスを止める／全員を止める

Renderのダッシュボード → 対象サービス → 左メニューの「**Environment**」タブから、
`SITE_USERS` の値を編集して保存します（保存すると自動で再デプロイされ、
数十秒〜1分程度で反映されます）。

- **特定の1人だけ止めたい** →
  `SITE_USERS` からその人の `ユーザー名:パスワード` の部分だけを削除して保存。
  他の人は今まで通り使えます。
  例: `yamada` を止めたい場合、
  `tanaka:8x2kQmZp,sato:p9Lm3vBq,yamada:qR7wZ2xT`
  → `tanaka:8x2kQmZp,sato:p9Lm3vBq` に変更。
- **新しく1人追加したい** → 同じ要領でカンマ区切りに追加するだけです。
- **今まで共有した相手も含め、全員のアクセスを止めたい** →
  `SITE_USERS` を空にする（全消し）か、値全体を新しいものに入れ替えて保存。
- **サービスごと一時停止したい** →
  `SITE_ENABLED` を `false` に変更して保存すると、URLにアクセスしても
  「現在このツールは一時停止中です」という画面が表示されるだけになります。
  再開したいときは `true` に戻してください。
- **完全に止めたい / 削除したい** →
  Renderのダッシュボードでサービス自体を Suspend（一時停止）または
  削除することもできます。

---

## 補足：ローカルでのパスワード保護テスト

デプロイ前に、自分のパソコンでパスワード保護の動作を確認することもできます。

```bash
# Windows PowerShellの場合
$env:SITE_USERS = "tanaka:testpass1,sato:testpass2"
python app.py
```

`http://127.0.0.1:5000` にアクセスすると、ユーザー名・パスワードを
求められるはずです（`SITE_USERS` を設定しなければ、今まで通り
パスワード無しで使えます）。
